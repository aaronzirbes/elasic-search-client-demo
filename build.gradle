apply plugin: 'groovy'
apply plugin: 'application'
apply plugin: 'idea'

mainClassName = 'org.zirbes.elasticsearch.ElasticSearchDemo'

repositories {
    mavenCentral()
}

dependencies {
    compile 'org.codehaus.groovy:groovy-all:2.1.3'

    compile 'com.fasterxml.jackson.core:jackson-databind:2.1.3'
    compile 'com.spatial4j:spatial4j:0.3'
    compile('com.vividsolutions:jts:1.12') {
        exclude group: 'xerces', module: 'xercesImpl'
    }
    compile 'org.elasticsearch:elasticsearch-lang-groovy:2.2.0' // Groovy API
    compile 'org.elasticsearch:elasticsearch:1.4.0' // Java API

    testCompile 'org.spockframework:spock-core:0.7-groovy-2.0'
}
